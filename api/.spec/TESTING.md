# Validação

Gates: `php -l`, `php api/tests/integration.php`, integração HTTP local quando endpoints mudarem e `npm.cmd run typecheck && npm.cmd run build`. O teste temporário cobre a coluna e serialização de compra; no Windows pode restar um warning de cleanup por lock do SQLite após o `OK`.
